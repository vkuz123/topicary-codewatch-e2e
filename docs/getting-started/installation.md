---
title: "Installation"
status: published
---

Run `node -e "require('@acme/sdk')"` to verify the installation succeeded.

Install the Acme SDK using your preferred package manager.

npm install @acme/sdk

The SDK requires Node.js 18 or later. For TypeScript 

This is reusable R15 audit content inserted via component reference.

projects, types are included automatically.